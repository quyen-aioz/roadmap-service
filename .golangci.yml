run:
  timeout: 5m

linters:
  enable:
    - gofmt # enforce formatting
    - goimports
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - gosimple
    - revive # better than golint
    - typecheck
